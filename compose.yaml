version: '2.0'
services:
  frontend:
    image: myfrontend
    build:
      context: frontend
      dockerfile: Dockerfile
    ports:
      - 3000:3000
  backend:
    image: mybackend
    build:
      context: backend
      dockerfile: Dockerfile
    ports:
      - 5000:5000
